#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox
exec-once = waybar
exec-once = hyprctl setcursor 20

exec-once = sudo modprobe rtw88_8822ce # Load wifi driver
exec-once = ip link set wlan0 up # Enable wifi interface
